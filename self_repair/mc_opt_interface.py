import pandas as pd
def mc_results_from_configs(new_configs:pd.DataFrame) -> pd.DataFrame:
    return pd.DataFrame
def opt_optimization(new_configs:pd.DataFrame) -> pd.DataFrame:
    return pd.DataFrame